const { default: next } = require('next')

/**
 * @type {import('next').NextConfig}
 */
 const nextConfig = {
    /* config optimages.unoptimized = trueons here */

  }
  images.unoptimized = true  
  module.exports = nextConfig